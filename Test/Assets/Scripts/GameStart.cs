using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameStart : MonoBehaviour
{
    public List<Traveler> currentTraveler = new List<Traveler>();
    // Start is called before the first frame update
    void Start()
    {
        //currentTraveler.Add(new MaleTraveler("小明").male);
        //currentTraveler.Add(new MaleTraveler("小钢").male);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
